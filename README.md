# 📝 To-Do List App (Java Swing)

## Overview
This project is a simple **desktop To-Do List application** built using **Java Swing**.  
It allows users to add and remove tasks in a graphical interface, with support for keyboard shortcuts and buttons.

---

## Features
- ✅ Add tasks using the **Add** button or by pressing **Enter** in the text field.
- ❌ Remove selected tasks using the **Remove** button.
- 🎨 Color-coded buttons:
  - Add button → **Pink**
  - Remove button → **Cyan**
- 📜 Scrollable task list for managing multiple items.
- 🖥️ Clean and centered window layout.

---

## Requirements
- **Java Development Kit (JDK)** 8 or higher (you already have OpenJDK 25 LTS installed).
- Any IDE or editor that supports Java (e.g., Visual Studio Code, IntelliJ IDEA, Eclipse).

---

## How to Run
### Option 1: Run in VS Code
1. Open the project folder in **Visual Studio Code**.
2. Make sure you have the **Java Extension Pack** installed.
3. Open `ToDoApp.java`.
4. Click **Run** above the `main` method or press `Ctrl+F5`.

### Option 2: Run from Terminal
1. Save the file as `ToDoApp.java`.
2. Open a terminal in the project folder.
3. Compile the program:
   ```bash
   javac ToDoApp.java
